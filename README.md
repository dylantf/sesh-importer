# Importer

### Prerequisites:

- postgresql 17
- pg_config on path (`/usr/pgsql-17/bin`)
- CSV files at correct location with correct names (see app/Main.hs)

### Building:

- `cabal build`

### Running:

- `cabal run`
